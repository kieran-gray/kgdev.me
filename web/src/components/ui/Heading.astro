---
import AsciiHeader from '../feature/AsciiHeader.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<h1 class="sr-only">{title}</h1>
<div>
	<AsciiHeader text={title} ariaLabel={title} size="sm" />
</div>
