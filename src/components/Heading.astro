---
import AsciiHeader from './AsciiHeader.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<p class="sr-only">{title}</p>
<div>
	<AsciiHeader text={title} ariaLabel={title} size="sm" />
</div>
